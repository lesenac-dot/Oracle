# Oracle core

Este diretório (oracle-core/) conterá o conteúdo original do repositório Oracle.

Atualmente eu já adicionei vários snapshots nas pastas relacionadas (oracle-tuning, oracle-scripts, oracle-docs, oracle-labs).

Para preservar histórico completo dos arquivos originais é necessário executar os scripts em migration-scripts/ num PC com git-filter-repo.

Snapshot: este repositório contém apenas cópias (sem histórico) dos arquivos oriundos dos repositórios listados nos READMEs das pastas.
